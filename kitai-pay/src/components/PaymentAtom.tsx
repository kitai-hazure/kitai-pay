import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { TOKEN_OPTIONS } from '../constants/storage';

const PaymentAtom = () => {
  return (
    <View>
      <Text>PaymentAtom</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
export default PaymentAtom;
